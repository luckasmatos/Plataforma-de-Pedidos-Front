import React from "react"
import { Main as MainContainer } from './styles'

function Main({children}) {
    return (

        <MainContainer>{children}</MainContainer>      
    )
}

export default Main